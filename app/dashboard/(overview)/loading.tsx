import DashboardSkeleton from "../../ui/skeletons";
export default function Loading() {
  /**
   * Renders the loading state for the dashboard overview.
   * @returns The loading skeleton component.
   */
  return <DashboardSkeleton />;
}